В файле CardList.jsx в строке 29 вставляю {cards} был код такой:
export const CardList = () => {

  const { cards, setParentCounter, handleProductLike } = useContext(CardContext)
  console.log(cards)
console.log выдавал все нормально массив объектов

когда меня на такой код:

export const CardList = ({cards}) => {

  const { setParentCounter, handleProductLike } = useContext(CardContext)
  console.log(cards)

обсыпается DOM, а console.log(cards) пустой.

Такая же история была, когда в ДЗ 5 игрался с фильтрацией в App.js, тогда после того, как убрал фильтрацию, остались лишние скобки, исправил все заработало.

Старую версию функции  Арр специально оставил заремленной, чтобы была полная картина. 
